This path is for OCP internal runtime.  It is intended to hold all SSL/TLS type certificates needed to communicate between OCP clients and services, to kafka, and to the database.

This readme file ensures zip utilities preserve an empty directory.
