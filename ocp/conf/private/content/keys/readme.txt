This path is for Universal Job or Content Gathering runtime.  It is intended to hold key files (e.g. shared/private SSH key files) needed in protocol-based connections to desired endpoints.

This readme file ensures zip utilities preserve an empty directory.
